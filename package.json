{
  "name": "shepherds-pie-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch database.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}